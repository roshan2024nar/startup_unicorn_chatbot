"""
utils/ — shared pure helpers used across the entire project.

Modules:
    text_utils   — industry/city keyword maps, normalize functions, company_to_text()
    filter_utils — extract_filters(), sanitization patterns, routing keywords
    data_loader  — CSV and JSON loading with validation
"""
